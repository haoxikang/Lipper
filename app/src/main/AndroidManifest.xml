<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fallllllll.lipperwithkotlin">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="com.fallllllll.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.main.home.ShotsActivity"
            android:theme="@style/ShotsActivityTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginWebActivity"
            android:theme="@style/AppTheme" />

        <!--<activity android:name=".ui.shotbeandetail.ShotBeanDetailActivity"-->
        <!--android:theme="@style/ShotsDetailTheme"/>-->
        <!--<activity android:name=".ui.search.SearchActivity"-->
        <!--android:theme="@style/SearchActivityTheme"/>-->
        <activity
            android:name=".ui.search.SearchActivity"
            android:theme="@style/SearchActivityTheme" />
        <activity
            android:name=".ui.login.DribbbleLoginActivity"
            android:theme="@style/DribbbleLoginTheme" />
        <activity android:name=".ui.usercenter.UserCenterActivity" />
    </application>

</manifest>